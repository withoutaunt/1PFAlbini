import { Titulos20Directive } from './titulos20.directive';

describe('Titulos20Directive', () => {
  it('should create an instance', () => {
    const directive = new Titulos20Directive();
    expect(directive).toBeTruthy();
  });
});
